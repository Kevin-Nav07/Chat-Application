## User Story 1

1. user is prompted to login or sign up
2. user clicks login and types in correct credentials
3. backend verifies credentials and gives user a session
4. User is redirected to dashboard where all of their chats(Rooms)are loaded in. 
5. User sees unread notifications on a chat and clicks on the chat
6. UI redirects to the chat window to display the current messages and opens a websocket connection.
7. UI redirects to the chat window t

